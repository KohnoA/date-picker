export interface ThemeType {
  datePickerMaxWidth: string;

  fontSizes: {
    lg: string;
    md: string;
    sm: string;
  };

  colors: {
    blue: string;
    blueTransparent: string;
    red: string;
    greyLight: string;
    greyNormal: string;
    greyNormalAlt: string;
    greyDark: string;
    black: string;
    white: string;
    backdrop: string;
  };

  borderRadius: {
    high: string;
    low: string;
  };

  opacity: {
    low: string;
    high: string;
  };

  margin: {
    lg: string;
    sm: string;
  };

  duration: string;
}
